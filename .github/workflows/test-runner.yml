---
name: Runner Test

on:
  workflow_dispatch:

jobs:
  test:
    runs-on:
      - self-hosted
      - shared-runners

    steps:
      - run: |
          echo "Runner funcionando!"
          uname -a
          hostname
