apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: "gha-runner-scale"
  namespace: argocd
  labels:
    app.kubernetes.io/name: "gha-runner-scale"
    app.kubernetes.io/managed-by: argocd
    app.kubernetes.io/part-of: addons
spec:
  goTemplate: true
  generators:
    - clusters: {}
  template:
    metadata:
      name: "gha-runner-scale-{{ .name }}"
      labels:
        app.kubernetes.io/name: "gha-runner-scale"
        app.kubernetes.io/managed-by: argocd
        app.kubernetes.io/part-of: addons
    spec:
      project: addons
      destination:
        server: "{{ .server }}"
        namespace: "gha-runner-scale"
      source:
        repoURL: https://github.com/estudosdevops/homelab.git
        targetRevision: refactor/homelab
        path: "kubernetes/addons/gha-runner-scale"
        helm:
          releaseName: "gha-runner-scale"
          valueFiles:
            - values/common.yaml
            - "values/{{ .name }}.yaml"
          ignoreMissingValueFiles: true
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
          - ApplyOutOfSyncOnly=true
  templatePatch: |
    metadata:
      name: "gha-runner-scale-{{ .name }}"
    spec:
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
          - ApplyOutOfSyncOnly=true
