apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: "kube-state-metrics"
  namespace: argocd
  labels:
    app.kubernetes.io/name: "kube-state-metrics"
    app.kubernetes.io/managed-by: argocd
    app.kubernetes.io/part-of: addons
spec:
  goTemplate: true
  generators:
    - clusters: {}
  template:
    metadata:
      name: "kube-state-metrics-{{ .name }}"
      labels:
        app.kubernetes.io/name: "kube-state-metrics"
        app.kubernetes.io/managed-by: argocd
        app.kubernetes.io/part-of: addons
    spec:
      project: addons
      destination:
        server: "{{ .server }}"
        namespace: "kube-system"
      source:
        repoURL: https://github.com/estudosdevops/homelab.git
        targetRevision: main
        path: "kubernetes/addons/kube-state-metrics"
        helm:
          releaseName: "kube-state-metrics"
          valueFiles:
            - values/common.yaml
            - "values/{{ .name }}.yaml"
          ignoreMissingValueFiles: true
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
          - ApplyOutOfSyncOnly=true
  templatePatch: |
    metadata:
      name: "kube-state-metrics-{{ .name }}"
    spec:
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
          - ApplyOutOfSyncOnly=true
