apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: "kubechecks"
  namespace: argocd
  labels:
    app.kubernetes.io/name: "kubechecks"
    app.kubernetes.io/managed-by: argocd
    app.kubernetes.io/part-of: addons
spec:
  goTemplate: true
  generators:
    - clusters: {}
  template:
    metadata:
      name: "kubechecks-{{ .name }}"
      labels:
        app.kubernetes.io/name: "kubechecks"
        app.kubernetes.io/managed-by: argocd
        app.kubernetes.io/part-of: addons
    spec:
      project: addons
      destination:
        server: "{{ .server }}"
        namespace: "kubechecks"
      source:
        repoURL: https://github.com/estudosdevops/homelab.git
        targetRevision: main
        path: "kubernetes/addons/kubechecks"
        helm:
          releaseName: "kubechecks"
          valueFiles:
            - values/common.yaml
            - "values/{{ .name }}.yaml"
          ignoreMissingValueFiles: true
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
          - ApplyOutOfSyncOnly=true
  templatePatch: |
    metadata:
      name: "kubechecks-{{ .name }}"
    spec:
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
          - ApplyOutOfSyncOnly=true
