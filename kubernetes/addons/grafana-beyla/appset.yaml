apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: "grafana-beyla"
  namespace: argocd
  labels:
    app.kubernetes.io/name: "grafana-beyla"
    app.kubernetes.io/managed-by: argocd
    app.kubernetes.io/part-of: addons
spec:
  goTemplate: true
  generators:
    - clusters: {}
  template:
    metadata:
      name: "grafana-beyla-{{ .name }}"
      labels:
        app.kubernetes.io/name: "grafana-beyla"
        app.kubernetes.io/managed-by: argocd
        app.kubernetes.io/part-of: addons
    spec:
      project: addons
      destination:
        server: "{{ .server }}"
        namespace: "grafana-beyla"
      source:
        repoURL: https://github.com/estudosdevops/homelab.git
        targetRevision: feat/grafana-beyla
        path: "kubernetes/addons/grafana-beyla"
        helm:
          releaseName: "grafana-beyla"
          valueFiles:
            - values/common.yaml
            - "values/{{ .name }}.yaml"
          ignoreMissingValueFiles: true
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
          - ApplyOutOfSyncOnly=true
  templatePatch: |
    metadata:
      name: "grafana-beyla-{{ .name }}"
    spec:
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
          - ApplyOutOfSyncOnly=true
